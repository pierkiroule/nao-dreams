export function generateLocalDream(contributions, seed) { const symbols=contributions.flatMap((contribution)=>contribution.answers.map((answer)=>answer.symbol)); const echo=`Dans l'Écho, ${symbols.slice(0,5).join(" ") || "✨"} répondent à la mer : une ville invisible apprend à respirer dans la lumière de celles et ceux qui l'ont écoutée.`; return { original:seed, echo:{title:"L'Écho des cinq écouteurs",text:echo,symbols}, symbols }; }
