import { useState } from "react";
import Button from "../components/Button";
export default function Seeds({ actions }) { const [count,setCount]=useState(2); return <section className="page seeds-page"><p className="eyebrow">Rêve partagé</p><h2 className="page-title">Combien de graines veux-tu inviter ?</h2><p className="page-text">Elles viennent de rêves précédents et nourriront doucement le tien.</p><div className="seed-picker" role="group" aria-label="Nombre de graines">{[0,1,2,3].map((number)=><button key={number} type="button" className={count===number?"is-selected":""} aria-pressed={count===number} onClick={()=>setCount(number)}><strong>{number}</strong><span>graine{number>1?"s":""}</span></button>)}</div><div className="credit-note"><span>✦</span><p><strong>Simulation IA</strong><br />La génération consommera 3 crédits sur tes 12 crédits achetés.</p></div><Button onClick={()=>actions.saveSeeds(count)}>Générer mon rêve · 3 crédits</Button></section> }
