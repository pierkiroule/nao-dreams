export default function Layout({ children, credits, localOnly, toggleLocal }) { return <main className="app-shell"><header className="brand"><h1>NAO SOUCY<span>•°</span></h1><div className="brand-meta"><span>{credits} crédit{credits > 1 ? "s" : ""}</span><button className="local-toggle" onClick={toggleLocal}>{localOnly ? "Mode totalement local" : "Empreinte minimisée"}</button></div></header>{children}</main>; }
