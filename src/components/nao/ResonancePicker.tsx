const choices=["Une image","Un mot","Une sensation","Une question","Rien de précis"];
type Props={value:string;word:string;onWordChange:(value:string)=>void;onChange:(value:string)=>void;onRelease:()=>void};
export default function ResonancePicker({value,word,onWordChange,onChange,onRelease}:Props){return <section className="resonance-picker"><h2>Qu’est-ce qui reste avec toi&nbsp;?</h2><div>{choices.map(choice=><button key={choice} aria-pressed={value===choice} onClick={()=>onChange(value===choice?"":choice)}>{choice}</button>)}</div>{value&&value!=="Rien de précis"&&<input value={word} maxLength={32} onChange={event=>onWordChange(event.target.value)} placeholder="Un mot, si tu le souhaites…" aria-label="Résonance personnelle facultative"/>}<button className="release-dream" onClick={onRelease}>Libérer la bulle dans le Grand O•° <span>○</span></button></section>}
