type Culture={id:string;culture:string;emojis:string[];summary:string};
type Props={culture:Culture;selected:boolean;position:{x:number;y:number};onSelect:(culture:Culture)=>void};
export default function CultureNode({culture,selected,position,onSelect}:Props){const name=culture.culture.split(/[,(]/)[0].trim();return <button className={`culture-node${selected?" selected":""}`} style={{"--node-x":`${position.x}%`,"--node-y":`${position.y}%`} as React.CSSProperties} onClick={()=>onSelect(culture)} aria-pressed={selected} aria-label={`${culture.emojis[0]}, ${name}`}><span aria-hidden="true">{culture.emojis[0]}</span><small>{name}</small></button>}
