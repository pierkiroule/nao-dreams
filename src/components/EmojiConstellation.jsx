const positions = [[50,8],[24,16],[76,17],[10,37],[39,34],[64,35],[90,39],[25,53],[51,53],[76,55],[13,72],[40,78]];
export default function EmojiConstellation({ emojis, selected, onSelect, dissolving }) { return <div className={`emoji-constellation ${dissolving ? "is-dissolving" : ""}`}>{emojis.map((emoji,index) => <button key={`${emoji.symbol}-${index}`} type="button" className={`dream-emoji ${selected?.symbol === emoji.symbol ? "is-selected" : ""}`} style={{ left:`${positions[index][0]}%`, top:`${positions[index][1]}%`, animationDelay:`-${index * .45}s` }} aria-label={emoji.label} aria-pressed={selected?.symbol === emoji.symbol} onClick={() => onSelect?.(emoji)}><span>{emoji.symbol}</span>{selected?.symbol === emoji.symbol && <em>{emoji.label}</em>}</button>)}</div>; }
