export default function DreamProgress({ count, threshold }) { const percentage=Math.min(100,Math.round((count/threshold)*100)); return <div className="dream-progress"><div className="abstract-stars" aria-hidden="true">{Array.from({length:12},(_,i)=><i key={i} style={{left:`${8+(i*31)%84}%`,top:`${12+(i*47)%72}%`,opacity:i<count?1:.24}} />)}</div><p><strong>{count} fragments</strong> ont déjà rejoint le rêve.</p><div className="progress-track"><span style={{width:`${percentage}%`}} /></div><small>Révélation à {threshold} fragments</small></div>; }
