import type {DreamFragment} from "../types/dream.ts";
import {dreamBubbleSchema} from "../schemas/dreamBubble.schema.ts";
export const FORBIDDEN_DREAM_WORDS=/\b(inconscient|signifie|guérison|trauma|diagnostic|âme|destinée|lâcher prise|lumière intérieure)\b/i;
export function fragmentWordCount(fragment:DreamFragment){return fragment.text.replace(/[«»….—]/g," ").trim().split(/\s+/).filter(Boolean).length}
export function validateDreamBubble(value:unknown){const bubble=dreamBubbleSchema.parse(value);const ids=new Set(bubble.fragmentIds);if(ids.size!==bubble.fragments.length)throw new Error("Fragments dupliqués");for(const fragment of bubble.fragments){if(fragmentWordCount(fragment)>18)throw new Error(`Fragment trop long: ${fragment.id}`);if(FORBIDDEN_DREAM_WORDS.test(fragment.text))throw new Error(`Vocabulaire interdit: ${fragment.id}`)}for(let i=1;i<bubble.fragments.length;i++){const before=bubble.fragments[i-1].grammaticalShape,after=bubble.fragments[i].grammaticalShape;if(before==="dialogue"&&after==="dialogue")throw new Error("Deux dialogues consécutifs");if(before==="enumeration"&&after==="enumeration")throw new Error("Deux énumérations consécutives")}return bubble}
