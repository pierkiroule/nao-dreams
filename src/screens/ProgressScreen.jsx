import Button from "../components/Button";
import DreamProgress from "../components/DreamProgress";
export default function ProgressScreen({ journey, onReveal, onTransmit, onReset }) { const ready=journey.status === "ready"; return <section className="page progress-screen"><p className="eyebrow">L'odyssée continue</p><h2 className="page-title">{ready ? "The dream is ready." : "Le rêve continue son voyage."}</h2><DreamProgress count={journey.contributionCount} threshold={journey.revealThreshold}/>{ready && <p className="page-text">Tous les signes ont été réunis. Le rêve peut maintenant être révélé.</p>}<Button onClick={ready ? onReveal : onTransmit}>{ready ? "Révéler le rêve" : "Transmettre Noa"}</Button><button className="quiet-action" onClick={onReset} type="button">Réinitialiser le rêve démo</button></section>; }
