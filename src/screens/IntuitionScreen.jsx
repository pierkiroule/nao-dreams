import Button from "../components/Button";
export default function IntuitionScreen({ choices, selected, onSelect, onContinue }) { return <section className="page intuition-screen"><p className="eyebrow">L'intuition</p><h2 className="page-title">Quel signe était déjà là ?</h2><p className="page-text">Écoute la scène un instant, puis suis ton intuition.</p><div className="intuition-choices">{choices.map((choice)=><button key={choice.symbol} type="button" className={selected?.symbol===choice.symbol?"is-selected":""} aria-pressed={selected?.symbol===choice.symbol} onClick={()=>onSelect(choice)}><span>{choice.symbol}</span><small>{choice.label}</small></button>)}</div><Button disabled={!selected} onClick={onContinue}>Révéler la trace</Button></section>; }
