import Button from "../components/Button";
export default function RiddleScreen({ riddle,index,total,answer,onChoose,onNext }) { return <section className="page riddle-screen"><p className="eyebrow">Énigme {index+1} / {total}</p><h2 className="page-title riddle-text">« {riddle.text} »</h2><div className="intuition-choices">{riddle.choices.map((symbol)=><button key={symbol} className={answer?.symbol===symbol?"is-selected":""} type="button" onClick={()=>onChoose(symbol)}><span>{symbol}</span></button>)}</div>{answer&&<p className="noa-response">{answer.symbol===riddle.answer ? "Intéressant…" : "Tu m'ouvres une autre piste…"}</p>}<Button disabled={!answer} onClick={onNext}>{index+1===total?"Écouter ma trace":"Une autre énigme"}</Button></section>; }
