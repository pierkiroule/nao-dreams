import Button from "../components/Button";
export default function IntuitionRevealScreen({ target, guessed, onContinue }) { const correct=target.emoji===guessed.symbol; return <section className="page intuition-reveal-screen"><p className="eyebrow">La trace répond</p><div className="consequence-symbol">{target.emoji}</div><h2 className="page-title">{correct ? "Tu as senti la bonne trace." : "Le rêve gardait un autre signe."}</h2><p className="scene-fragment">{correct ? `${target.emoji} — ${target.label} — était déjà entré dans le rêve.` : `C'était ${target.emoji} — ${target.label}. Le rêve ne se laisse pas toujours deviner.`}</p><Button onClick={onContinue}>Ajouter mon signe</Button></section>; }
